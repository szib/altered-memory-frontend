// const ticking = (context) => {
//   const interval = setInterval(() => {
//     context.time += 1;
//     // assign({ time: context => context.time + 1 });
//   }, context.timerInterval);
//   return () => clearInterval(interval);
// };

// const activities = {
//   ticking,
// };

// export default activities;
